#include <stdio.h>

float c = 1;

long long func(int aF, char hF) {
    return a;
}

int main() {
    auto autoV = 1;
    long long longModifier = 0;
    int z, x, y = 10 + 13;
    float d = 1.5;
    xA, zA = 5;

    if (int iInitIf = 0) {
        char q = 'q';
        zA = 1;
        if (z < 0) {
            aInnerIf = a;
        }
    } else if (z > 0) {
        zA = 2;
    } else if (z == 1) {
        zA = 4;
    } else {
        zA = 3;
    }

    for (int iInitFor = 0; i < n; ++i) {
        aFor1 += 1;
        aFor2 += 1;
        for (int i = 0; i < n; ++i) {
            aFor3 += 1;
            if (z < 0) {
                a3 = a;
            }
        }
    }

    while (true) {
        aWhile = 1;
        int bWhile = 'c';
    }

    do {
        aDoWhile = 1;
        int bDoWhile = 'c';
    } while (true);
}
