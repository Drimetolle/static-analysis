#include <stdio.h>

class A {
public:
    int innerA;

    int print() const {
        return innerA;
    }
};

float c = 1;

int func(int a) {
    return a;
}

int main() {
    int z, x, y = 10 + 13;
    float d = 1.5;
    xA, zA = 5;

    if (z < 0) {
        char q = 'q';
        zA = 1;
    } else if (z > 0) {
        zA = 2;
    } else if (z == 1) {
        zA = 4;
    } else {
        zA = 3;
    }
}
